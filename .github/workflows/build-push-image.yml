#Pour tout push dans le repo
#- déclencher un build d'une nouvelle image docker en utilisant le Dockerfile 
#- et faire un psuh vers dockerHub

name: Build and Push Image
on: [push]
jobs:
  build_and_push_image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login to Docker Registry
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: ${{ secrets.svinux }}
          password: ${{ secrets.W$#7EnK8rr*X4&xs2x!L9u8bLYE3@v8P }}
      - name: Build and push
        uses: docker/build-push-action@v2
        with:
            context: .
            file: ./Dockerfile
            push: true
            tags: ghcr.io/svinux/getting-started:latest
